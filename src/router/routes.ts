export enum ROUTE {
    HOME = '/',
    COUNTRY_DETAILS = 'country/:name',
    FAVORITES = 'favorites',
    SIGN_IN = 'sign-in',
    SIGN_UP = 'sign-up',
    SEARCH = 'search',
    NOT_FOUND = '*',
}
